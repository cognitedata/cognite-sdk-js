{
  "packages": [
    "packages/**",
    "samples/**"
  ],
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "version": {
      "ignoreChanges": ["**/__tests__/**", "**/*.unit.spec.ts"],
      "message": "chore(release): publish new package versions [skip ci]",
      "allowBranch": "v1"
    }
  }
}
