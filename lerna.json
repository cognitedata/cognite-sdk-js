{
  "packages": [
    "packages/**",
    "samples/*"
  ],
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "version": {
      "ignoreChanges": ["**/__tests__/**", "**/*.spec.ts", "**/*.test.ts"],
      "message": "chore(release): publish new package versions [skip ci]",
      "allowBranch": "v1"
    }
  }
}
