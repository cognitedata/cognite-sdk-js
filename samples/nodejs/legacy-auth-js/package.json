{
  "name": "legacy-auth-js-nodejs-sample",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "dependencies": {
    "@cognite/sdk": "*"
  },
  "scripts": {
    "test": "node quickstart.js && yarn test-for-await",
    "test-for-await": "if yarn run check-node-version --node '>= 10'; then node for-await.js; else true; fi"
  },
  "devDependencies": {
    "check-node-version": "^4.0.0"
  }
}
