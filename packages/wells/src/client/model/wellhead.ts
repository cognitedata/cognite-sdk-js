/**
 * Explains the position of the wellhead
 * @export
 * @interface Wellhead
 */
export interface Wellhead {
  /**
   * @type number
   * @memberOf WellHeadLocation
   */
  x: number;
  /**
   * @type number
   * @memberOf WellheadLocation
   */
  y: number;
  /**
   * @type string
   * @memberOf WellheadLocation
   */
  crs: string;
  /**
   * @type number
   * @memberOf WellheadLocation
   */
  id: number;
}
